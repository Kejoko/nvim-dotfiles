# Neovim Config

Things to fix:
- highlighting isn't working correctly
    - const variables are not currently italicised or colored differently than non-const

Plugins to fix:
- nvim-cmp
    - autocomplete is not currently working
