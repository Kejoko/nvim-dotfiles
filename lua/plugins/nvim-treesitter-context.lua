-- So we can get the context of our current scope to show up at the top of the window.
-- This is useful so when we are scrolling we can see the function definition that our
-- cursor is in, instead of having to scroll way up.
return {
    "nvim-treesitter/nvim-treesitter-context",
}
